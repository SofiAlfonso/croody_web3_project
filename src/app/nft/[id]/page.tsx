/**
 * Pagina de detalle de NFT
 * Muestra informacion completa del NFT y opciones de compra
 */
export default function NFTDetailPage({ params }: { params: { id: string } }) {
  // TODO: Implementar
  return (
    <div>
      <h1>NFT #{params.id}</h1>
      {/* TODO: NFTDetails, BuyButton o info de subasta */}
    </div>
  );
}
